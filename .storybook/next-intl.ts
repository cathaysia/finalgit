import cn from '../messages/cn.json';
import en from '../messages/en.json';

const messagesByLocale = { en, cn };

const nextIntl = {
  defaultLocale: 'en',
  messagesByLocale,
};

export default nextIntl;
